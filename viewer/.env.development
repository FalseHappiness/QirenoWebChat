# 可不尾随 “/”
VITE_API_BASE_URL=http://sitehost:58471
VITE_WS_URI=ws://sitehost:58471/ws/frontend
# true 检测后端，false 不检测
VITE_BACKEND_DETECTOR=true
# 路径会直接拼接 VITE_BASE 后，所以必须要 “/” 结尾。最好 “/” 开头，使用绝对路径，否则会变成相对路径
VITE_BASE=/